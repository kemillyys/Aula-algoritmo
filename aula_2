#include <stdio.h>
#include <stdlib.h>

int main()
{
    /*int n1, n2;
    double media;

    printf("Informe o primeiro número:");
    scanf("%d", &n1);
    printf("Informe o segundo número:");
    scanf("%d", &n2);

    media = (n1 + n2) / 2.0;
    printf("A média é: %lf\n", media);
    */




    /*int numero, quadrado;
    printf("Informe um número:");
    scanf("%d", &numero);
    quadrado = numero + numero;
    printf("O quadrado do número %d é: %d\n", numero, quadrado);*/

    /*double saldoCliente, saldoAjustado, reajuste;

    printf("Informe o seu saldo:");
    scanf("%lf", &saldoCliente);

    reajuste = saldoCliente / 100;
    saldoAjustado = saldoCliente + reajuste;

    printf("O saldo com o reajuste é: %lf\n", saldoAjustado);*/




    /*double preco, novoPreco, desconto;

    printf("Infome o valor:");
    scanf("%lf", &preco);

    desconto = preco * 0.09;
    novoPreco = preco - desconto;

    printf("O preço com desconto é: %lf", novoPreco);*/




    /*char v1, v2, v3;
    printf("Informe o primeiro caractere:");
    scanf("%c", &v1);

    printf("Informe o segundo caractere:");
    scanf(" %c", &v2);

    printf("Informe o terceiro caractere:");
    scanf(" %c", &v3);

    printf("Os caracteres selecionados foram: %c %c %c", v1, v2, v3);*/





    /*int num1, num2, num3, resultado;

    printf("Informe o primeiro numero:");
    scanf("%d", &num1);

    printf("Informe o segundo numero:");
    scanf("%d", &num2);

    printf("Informe o terceiro numero:");
    scanf("%d", &num3);

    resultado = 2 * ((num1 - num3) / 8) - num2 * 5;

    printf("O resultado da equação é: %d\n", resultado);*/




    int num1, num2, resultadoSoma, resultadoMultiplicacao, resultadoSubtracao, resultadoDivisao, resultadoResto;

    printf("Informe o primeiro número:");
    scanf("%d", &num1);

    printf("Informe o segundo número:");
    scanf("%d", &num2);

    resultadoSoma = num1 + num2;

    resultadoMultiplicacao = num1 * num2;

    resultadoSubtracao = num1 - num2;

    resultadoDivisao = num1 / num2;

    resultadoResto = num1 % num2;

    printf("O resultado da soma é: %d\n O resultado da multiplicação é: %d\n O resultado da subtração é: %d\n O resultado da divisão é: %d\n O resto da divisão é %d", resultadoSoma, resultadoMultiplicacao, resultadoSubtracao, resultadoDivisao, resultadoResto);

    return 0;
}
